# Frontend Feature Template

TASK: [feature name]

TARGET FILE: [apps/web/src/pages/xxx.tsx OR apps/web/src/components/xxx.tsx]

IMPLEMENT:
[list what to build]

RULES:
- Modify existing file only — do not rewrite unchanged sections
- No explanations, no summaries
- React Query for data fetching
- React Hook Form + Zod for forms
- shadcn components only
- Must include: loading skeleton, empty state, error state
- Indian currency format for all amounts
- Responsive layout required
